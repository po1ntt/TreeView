<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TreeView.MainPage"
             xmlns:controltree="clr-namespace:TreeView.Controls.TreeView"
             Background="#111"
             Title=""
             Shell.NavBarIsVisible="False"
             xmlns:vm="clr-namespace:TreeView.Controls.ViewModelsControl">
    <ContentPage.BindingContext>
        <vm:TreeViewModel></vm:TreeViewModel>
    </ContentPage.BindingContext>
    <ScrollView>
 
        <VerticalStackLayout>

        <ScrollView Orientation="Both" VerticalScrollBarVisibility="Always" HorizontalScrollBarVisibility="Always">
            <controltree:TreeView>

            </controltree:TreeView>
        </ScrollView>
           
        </VerticalStackLayout>

    </ScrollView>
</ContentPage>
